import 'package:flutter/material.dart';

class Colores {
  // Define el color amarillo reutilizable
  static const Color amarillo = Color.fromRGBO(255, 202, 68, 1);

  // Otros colores pueden ir aquí
  static const Color negro = Color(0xFF222327);
}
